server:
  protocol: http
  host: localhost
  port: 9200
  connection-timeout: 5s
  tomcat:
    max-threads: 100
    max-swallow-size: 1MB
    max-http-post-size: 1MB

spring:
  data:
    mongodb:
      #      host: @authentication.database.host@
      #      database: @authentication.database.name@
      #      port: @authentication.database.port@
      uri: @authentication.database.uri@

  security:
    oauth2:
      client:
        registration:
          facebook:
            clientId: 2318941121751144
            clientSecret: c115893acdd55be9834176570572a767
            redirectUriTemplate: http://localhost:9200/oauth2/callback/{registrationId}
            redirect-uri: http://localhost:9200/oauth2/callback/{registrationId}
            authorize-uri: http://localhost:9200/oauth2/authorize/facebook
            scope:
              - email
              - public_profile
          google:
            clientId: @google.cliendId@
            clientSecret: @google.client.secret@
            redirectUriTemplate: @oauth.redirect@
            authorize-uri: http://localhost:9200/oauth2/authorize/google
            scope:
              - email
              - profile
              - openid
        provider:
          facebook:
            authorizationUri: https://www.facebook.com/v3.0/dialog/oauth
            tokenUri: https://graph.facebook.com/v3.0/oauth/access_token
            userInfoUri: https://graph.facebook.com/v3.0/me?fields=id,first_name,middle_name,last_name,name,email,verified,is_verified,picture.width(250).height(250)

app:
  auth:
    tokenSecret: 926D96C90030DD58429D2751AC1BDBBC
    tokenExpirationMsec: @tokenExpirationMsec@
  oauth2:
    authorizedRedirectUris:
      - http://localhost:9004/oauth2/redirect
